"use client"

import type React from "react"
import { useEffect, useRef } from "react"

const AnimatedBackground: React.FC = () => {
  // 目前组件为空，但需要返回一个有效的ReactNode
  return null;
}

export default AnimatedBackground

